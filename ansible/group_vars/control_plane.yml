---
# ============================================================
# CONTROL PLANE VARIABLES
# ============================================================

# Override these per environment as required.
aws_region: "us-east-1"

cluster_name: "k8s-cluster"

ssm_join_parameter: "/k8s/join-command"

control_plane_publish_join_command: true
