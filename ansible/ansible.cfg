[defaults]
roles_path = ./roles
# AWS EC2 dynamic inventory is the primary inventory.
# Static inventory remains available through hosts.ini.example.
inventory = ./inventory/aws_ec2.yml

host_key_checking = False
interpreter_python = auto_silent
timeout = 30
stdout_callback = default
bin_ansible_callbacks = True

[ssh_connection]
pipelining = True

